#### Диапазон сканирования сети

        

```
MuraMitchi@htb[/htb]$ sudo nmap 10.129.2.0/24 -sn -oA tnet | grep for | cut -d" " -f5 10.129.2.4 10.129.2.10 10.129.2.11 10.129.2.18 10.129.2.19 10.129.2.20 10.129.2.28
```

| **Параметры сканирования** | **Описание**                                                        |
| -------------------------- | ------------------------------------------------------------------- |
| `10.129.2.0/24`            | Целевой диапазон сети.                                              |
| `-sn`                      | Отключает сканирование портов.                                      |
| `-oA tnet`                 | Сохраняет результаты во всех форматах, начинающихся с имени 'tnet'. |
  ## Сканировать список IP-адресов
		

```
MuraMitchi@htb[/htb]$ sudo nmap -sn -oA tnet -iL hosts.lst | grep for | cut -d" " -f5 10.129.2.18 10.129.2.19 10.129.2.20
```

| **Параметры сканирования** | **Описание**                                                                         |
| -------------------------- | ------------------------------------------------------------------------------------ |
| `-sn`                      | Отключает сканирование портов.                                                       |
| `-oA tnet`                 | Сохраняет результаты во всех форматах, начинающихся с имени 'tnet'.                  |
| `-iL`                      | Выполняет заданные проверки целевых объектов из предоставленного списка 'hosts.lst'. |

## Сканирование нескольких IP-адресов

Также может случиться так, что нам потребуется просканировать лишь небольшую часть сети. Альтернативой использованному в прошлый раз методу является указание нескольких IP-адресов.

        shellsession
`MuraMitchi@htb[/htb]$ sudo nmap -sn -oA tnet 10.129.2.18 10.129.2.19 10.129.2.20| grep for | cut -d" " -f5 10.129.2.18 10.129.2.19 10.129.2.20`

Если эти IP-адреса находятся рядом, мы также можем определить диапазон в соответствующем октете.

        shellsession
`MuraMitchi@htb[/htb]$ sudo nmap -sn -oA tnet 10.129.2.18-20| grep for | cut -d" " -f5 10.129.2.18 10.129.2.19 10.129.2.20`

## Сканирование одного IP-адреса

Прежде чем сканировать хост на наличие открытых портов и служб, необходимо определить, активен он или нет. Для этого можно использовать тот же метод, что и раньше.

        shellsession

```
MuraMitchi@htb[/htb]$ sudo nmap 10.129.2.18 -sn -oA host  Starting Nmap 7.80 ( https://nmap.org ) at 2020-06-14 23:59 CEST Nmap scan report for 10.129.2.18 Host is up (0.087s latency). MAC Address: DE:AD:00:00:BE:EF Nmap done: 1 IP address (1 host up) scanned in 0.11 seconds
```

| **Параметры сканирования** | **Описание**                                                        |
| -------------------------- | ------------------------------------------------------------------- |
| `10.129.2.18`              | Выполняет заданное сканирование целевого объекта.                   |
| `-sn`                      | Отключает сканирование портов.                                      |
| `-oA host`                 | Сохраняет результаты во всех форматах, начинающихся с имени «host». |
```
MuraMitchi@htb[/htb]$ sudo nmap 10.129.2.18 -sn -oA host -PE --packet-trace 

Starting Nmap 7.80 ( https://nmap.org ) at 2020-06-15 00:08 CEST
SENT (0.0074s) ARP who-has 10.129.2.18 tell 10.10.14.2
RCVD (0.0309s) ARP reply 10.129.2.18 is-at DE:AD:00:00:BE:EF
Nmap scan report for 10.129.2.18
Host is up (0.023s latency).
MAC Address: DE:AD:00:00:BE:EF
Nmap done: 1 IP address (1 host up) scanned in 0.05 seconds
```

|**Параметры сканирования**|**Описание**|
|---|---|
|`10.129.2.18`|Выполняет заданное сканирование целевого объекта.|
|`-sn`|Отключает сканирование портов.|
|`-oA host`|Сохраняет результаты во всех форматах, начинающихся с имени «host».|
|`-PE`|Выполняет сканирование ping с использованием запросов ICMP Echo к целевому объекту.|
|`--packet-trace`|Отображает все отправленные и полученные пакеты.|
Ещё один способ определить, почему Nmap пометил наш объект как "живой", — это использовать `--reason`опцию "".

```
MuraMitchi@htb[/htb]$ sudo nmap 10.129.2.18 -sn -oA host -PE --reason  Starting Nmap 7.80 ( https://nmap.org ) at 2020-06-15 00:10 CEST SENT (0.0074s) ARP who-has 10.129.2.18 tell 10.10.14.2 RCVD (0.0309s) ARP reply 10.129.2.18 is-at DE:AD:00:00:BE:EF Nmap scan report for 10.129.2.18 Host is up, received arp-response (0.028s latency). MAC Address: DE:AD:00:00:BE:EF Nmap done: 1 IP address (1 host up) scanned in 0.03 seconds
```

|**Параметры сканирования**|**Описание**|
|---|---|
|`10.129.2.18`|Выполняет заданное сканирование целевого объекта.|
|`-sn`|Отключает сканирование портов.|
|`-oA host`|Сохраняет результаты во всех форматах, начинающихся с имени «host».|
|`-PE`|Выполняет сканирование ping с использованием запросов ICMP Echo к целевому объекту.|
|`--reason`|Отображает причину получения конкретного результата.|

#### Nmap - Отслеживание пакетов

        shellsession
`MuraMitchi@htb[/htb]$ sudo nmap 10.129.2.28 -p 21 --packet-trace -Pn -n --disable-arp-ping Starting Nmap 7.80 ( https://nmap.org ) at 2020-06-15 15:39 CEST SENT (0.0429s) TCP 10.10.14.2:63090 > 10.129.2.28:21 S ttl=56 id=57322 iplen=44  seq=1699105818 win=1024 <mss 1460> RCVD (0.0573s) TCP 10.129.2.28:21 > 10.10.14.2:63090 RA ttl=64 id=0 iplen=40  seq=0 win=0 Nmap scan report for 10.129.2.28 Host is up (0.014s latency). PORT   STATE  SERVICE 21/tcp closed ftp MAC Address: DE:AD:00:00:BE:EF (Intel Corporate) Nmap done: 1 IP address (1 host up) scanned in 0.07 seconds`

|**Параметры сканирования**|**Описание**|
|---|---|
|`10.129.2.28`|Сканирует указанный объект.|
|`-p 21`|Сканирует только указанный порт.|
|`--packet-trace`|Отображает все отправленные и полученные пакеты.|
|`-n`|Отключает разрешение DNS-запросов.|
|`--disable-arp-ping`|Отключает ARP-пинг.|

---

Из строки SENT видно, что мы ( `10.10.14.2`) отправили TCP-пакет с `SYN`флагом ( `S`) нашему целевому объекту ( `10.129.2.28`). В следующей строке RCVD видно, что целевой объект отвечает TCP-пакетом, содержащим флаги `RST`и `ACK`( `RA`). Флаги `RST`и `ACK`используются для подтверждения получения TCP-пакета ( `ACK`) и для завершения TCP-сессии ( `RST`).

#### Запрос

|**Сообщение**|**Описание**|
|---|---|
|`SENT (0.0429s)`|Указывает на операцию SENT программы Nmap, которая отправляет пакет целевому объекту.|
|`TCP`|Отображает протокол, используемый для взаимодействия с целевым портом.|
|`10.10.14.2:63090 >`|Здесь указывается наш IPv4-адрес и исходный порт, которые будут использоваться Nmap для отправки пакетов.|
|`10.129.2.28:21`|Отображает целевой IPv4-адрес и целевой порт.|
|`S`|Флаг SYN отправленного TCP-пакета.|
|`ttl=56 id=57322 iplen=44 seq=1699105818 win=1024 mss 1460`|Дополнительные параметры заголовка TCP.|

#### Ответ

|**Сообщение**|**Описание**|
|---|---|
|`RCVD (0.0573s)`|Указывает на получение пакета от целевого устройства.|
|`TCP`|Отображает используемый протокол.|
|`10.129.2.28:21 >`|Представляет собой IPv4-адрес цели и исходный порт, которые будут использоваться для ответа.|
|`10.10.14.2:63090`|Отображает наш IPv4-адрес и порт, на который будет отправлен ответ.|
|`RA`|Флаги RST и ACK отправленного TCP-пакета.|
|`ttl=64 id=0 iplen=40 seq=0 win=0`|Дополнительные параметры заголовка TCP.|
#### Подключить сканирование через TCP-порт 443

```
MuraMitchi@htb[/htb]$ sudo nmap 10.129.2.28 -p 443 --packet-trace --disable-arp-ping -Pn -n --reason -sT  Starting Nmap 7.80 ( https://nmap.org ) at 2020-06-15 16:26 CET CONN (0.0385s) TCP localhost > 10.129.2.28:443 => Operation now in progress CONN (0.0396s) TCP localhost > 10.129.2.28:443 => Connected Nmap scan report for 10.129.2.28 Host is up, received user-set (0.013s latency). PORT    STATE SERVICE REASON 443/tcp open  https   syn-ack Nmap done: 1 IP address (1 host up) scanned in 0.04 seconds
```